syntax = "proto3";

package test;

message TestRequest {
    string content = 1;
}

message TestResponse {
    string content = 1;
}

service Test {
    rpc Call(TestRequest) returns (TestResponse);
}

service TestStream {
    rpc Call(stream TestRequest) returns (stream TestResponse);
}